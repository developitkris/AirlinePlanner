@{
  Layout = "_Layout";
}

<h3>Cities</h3>

  <div>@if(Model.Count != 0)
  {
    <ul>
      @foreach (var city in Model)
      {
        <li><a href="/cities/@city.GetCityId()">@city.GetName()</a></li>
      }
    </ul>
  }
</div>

  <h4><a href="/cities/new">Add new city</a></h4>
  <h4><a href="/">Return to home page</a></h4>
